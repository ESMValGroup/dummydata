language: python
python:
  - "2.7"

virtualenv:
system_site_packages: true

before_install:
  - sudo apt-get update


# command to install dependencies
install:
  pip install netCDF4
  #- pip install matplotlib
  #- pip install scipy
  #- pip install sympy




script:
  - nosetests
#after_success:
#  - coveralls
